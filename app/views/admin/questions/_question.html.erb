<ul>
  <li>
    <b><%= question.title %></b>
    <%= button_to 'Show', admin_question_path(question)%>
    <%= button_to 'Edit', edit_admin_question_path(question) %>
    <%= button_to 'Delete', admin_question_path(question),
                         method: :delete,
                         data: { confirm: 'Are you sure?' }%>
  </li>
</ul>
