<h1>Edit Answer</h1>

<p>
  <%= @answer.question.title %>
</p>

<%= render 'form', answer: @answer %>

<%= button_to 'Show', @answer, method: :get %> |
<%= button_to 'Back', admin_question_path(@answer.question), method: :get %>
